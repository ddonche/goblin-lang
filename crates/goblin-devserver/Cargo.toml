[package]
name = "goblin-devserver"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Goblin development server wrapper (uses goblin-host)"

[dependencies]
goblin-host = { path = "../goblin-host" }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
